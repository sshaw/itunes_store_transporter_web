#!/bin/bash

set -e

root=$(dirname "$0")
padrino="${root}/padrino"

if [ ! -x "$padrino" ]; then
    echo $padrino is missing, please run the install script first: ruby setup.rb
    exit 1
fi

"$padrino" rake -c "$root/.." -e production jobs:${1:-work}
