- content_for(:title) do
  Status

%h2
  Status 

%p
  Retrieve status information about a previously uploaded package. 

- form_for @options, :status, :class => "form-horizontal" do |f|    
  = f.text_field :vendor_id, :label => "Vendor ID:"
  = f.select_priority

  = partial "shared/auth_fields", :object => f

  = f.actions do 
    = f.submit "Check Status"