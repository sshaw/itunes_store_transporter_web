- content_for :results do 
  .actions
    = link_to_view url(:job_metadata, job.id, :format => :xml)
    = link_to_download url(:job_metadata, job.id)

  = highlite(job.result, :xml)

= partial "jobs/results/result"