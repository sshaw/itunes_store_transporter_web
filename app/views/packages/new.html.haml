- content_for(:title) do
  New Package

%h2
  New Package

= form_for @package, url(:packages), :class => "form-horizontal" do |f|
  = partial "packages/fields", :object => f, :locals => { :accounts => @accounts }

  = f.actions do
    = f.submit "Create"
